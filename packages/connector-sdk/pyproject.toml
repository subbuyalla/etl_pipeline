[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "connector-sdk"
version = "0.1.0"
description = "Connector SDK interface — twin and real adapters implement the same contract"
requires-python = ">=3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
